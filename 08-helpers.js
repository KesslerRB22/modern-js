const tech = 'Node.js';
const array = ['Node', 'React','Json'];

const includes = tech.includes('.'); 
const includesArray = array.includes('React');

const startWith = tech.startsWith('Nod');

console.log({ startWith });